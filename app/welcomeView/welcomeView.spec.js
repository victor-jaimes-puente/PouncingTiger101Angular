'use strict';

describe('myApp.welcomeView module', function() {

  beforeEach(module('myApp.welcomeView'));

  describe('welcomeView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var welcomeViewCtrl = $controller('WelcomeViewCtrl');
      expect(welcomeViewCtrl).toBeDefined();
    }));

  });
});